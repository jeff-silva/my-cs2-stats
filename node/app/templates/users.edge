<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Usuários</title>
  </head>
  <body>
    @include('partials/reports')
    <br />

    <table border="1">
      <thead>
        <tr>
          <th>Username</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        @each(user in data)
        <tr>
          <td>{{ user.name }}</td>
          <td>
            @if(user.steamProfile)
            <a href="{{ user.steamProfile }}" target="_blank">Profile</a>
            @else
            <a href="{{ user.steamSearch }}" target="_blank">Search</a>
            @end
          </td>
        </tr>
        @end
      </tbody>
    </table>
  </body>
</html>
